Pane-header control. Minimum height is `--road-hit-min` (24px) — the as-built 9px/py-0.5 buttons are below any usable target.

```jsx
<ToggleButton tone="warn">◇ optionals bypassed</ToggleButton>
<ToggleButton disabled>↶ undo</ToggleButton>
```
