The rail's chip. Group chips are dashed + well-tinted so the rail uses the same containment grammar as the road.

```jsx
<NodeChip title="TCP vs UDP" domainColor="var(--domain-net)" linked />
<NodeChip title="Secure the channel" group count={4} />
```
