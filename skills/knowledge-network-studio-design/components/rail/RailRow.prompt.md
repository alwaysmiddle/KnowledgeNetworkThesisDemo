Rows of the projected route. Numbering must come from the SAME resolved walk the road badges use — that shared count is what makes the side-by-side readable.

```jsx
<ol style={{margin:0,padding:0,display:'flex',flexDirection:'column',gap:'var(--space-1)'}}>
  <RailRow step={3} title="TCP vs UDP" domainColor="var(--domain-net)" linked />
  <RailRow title="Secure the channel" group count={4} />
</ol>
```
