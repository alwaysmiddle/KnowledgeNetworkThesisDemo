One caret for every insertion point. If you find yourself rendering a second kind of drop mark, the drag model is wrong, not the mark.

```jsx
{hot && <DropLine width={150} />}
<svg><RoadArrow x={90} y1={40} y2={66} live /><BypassRail d={path} live={false} /></svg>
```
