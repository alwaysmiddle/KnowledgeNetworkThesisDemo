Fixed left slot carrying a block's status — use it on every road block so the number/mark column lines up across depths.

```jsx
<StatusGutter order={7} />
<StatusGutter optional />
<StatusGutter fork />
```

Status only. Actions belong in the block's right-hand ⋯ menu, never here.
