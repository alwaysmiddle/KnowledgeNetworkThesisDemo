{
  "$note": "Sync stamp for the vendored KnowledgeNetwork Design System snapshot. The DS is a Claude Design project with NO native version/release and NO per-file hashes in its manifest, so this file IS the diffable version marker of what state we adopted. To check for drift: re-read the DS via /design-sync, re-hash, and compare against `tokens.files` below; to adopt a change, re-vendor and update the hashes + syncedAt here. Graduating to a properly published/semver'd dependency is tracked in issue #66. PATHS REPOINTED 2026-08-18: the DS deleted its export/ folder that day (it was a hand-written second copy that nothing read), so every `from` in this file now names an AUTHORED path in the design project. tailwindTheme.from moved export/tailwind/kn-theme.css -> tailwind/kn-theme.css; the log formerly at export/SYNC.md is now sync-log.md; the issue list formerly at export/ISSUES.md is now issues.md. Component `from`/`contract` paths were already authored paths and did not move.",
  "source": {
    "kind": "claude-design-project",
    "projectId": "0ec749ff-3549-4c16-9d1f-09ab8e8d9039",
    "projectName": "KnowledgeNetwork Design System",
    "namespace": "KnowledgeNetworkDesignSystem_0ec749",
    "hasNativeVersion": false,
    "accessedVia": "the DesignSync tool's read methods (list_files / get_file), driven by the project skill at .claude/skills/design-sync. NOT the bundled /design-sync skill, which pushes repo -> claude.ai and would overwrite the design project"
  },
  "syncedAt": "2026-08-12",
  "verifiedAt": "2026-08-18 (#111): base.css RE-VENDORED from the DS and adopted — the data-kn-hover recipe, appended at the foot, plus the four-line header comment that announces it. Verified rather than assumed: the PRE-sync local file reproduced its recorded fingerprint de6bd15d… exactly, which proves both the hash recipe and that our copy was byte-identical to the DS before this run, so the diff IS the whole upstream change. New fingerprint e6eba0a3…. All eleven tokens the hook consumes were checked to exist here first (--surface-hover, --surface-press, --surface-hover-raised, --border-rule, --text-1, --text-2, --transition-wash, --state-danger, --state-danger-wash, --berry-600, --opacity-disabled) — var() on a missing name fails silently to nothing, which is the one way this could have shipped broken and green. THE HOOK IS INERT AS SHIPPED: no element in src/ sets data-kn-hover, so there is no visual change in this commit. Replacing hand-rolled useState hover with the attribute is H10 and is deliberately NOT done here. THE OTHER SIX TOKEN FILES WERE NOT RE-FETCHED OR RE-HASHED THIS RUN — the DS asserts in design-sync.md that base.css was the only outstanding token file and that it is 'otherwise byte-identical to ours'; that is an assertion taken on trust, not a check performed here. Receipt written to the design project at receipts/<sha>.md. PREVIOUS ENTRY: 2026-08-17: the DS moved (github.md stamped 2026-08-17T02:40Z, after our merge a678ffe) and four contracts were re-read and ported — VersionedGroup (ungroupConfirmLabel + onClose), TreeRow (caretStyle + depth), PaneHeader (the three pane rules + the actions recipe) and NodeChip (onDelete, already correct here). TOKENS WERE NOT RE-HASHED THIS RUN — the 2026-08-16 check below is the last one. The DS's standing --border-hair item is STALE: colors.css:108 is already rgba(53, 49, 42, 0.17), the DS's own value. The DS also reported four things it saw wrong on our shipped road; all four verified real, and the three that are ADOPTIONS rather than ports are left to issues. 2026-08-17 SECOND PASS: the DS-side prose was PORTED rather than only the behaviour — VersionedGroupProps went from 12 documented props of 50 to 42, taking the DS .d.ts wording verbatim for the 30 that had none. Two findings came out of doing it. (a) The `selected` doc here still described the pre-2026-08-16 head-only 1px ring; the DRAWING had been re-ported that day (VersionedGroup.tsx:1216 draws the whole-card 2px outline at 2px offset, exactly the DS spec) and the comment above it had not — our own contract lag, the same class we keep reporting upstream. Corrected, with the history left in the comment. (b) `defaultSelected` / `onSelectedChange` are genuinely NOT ported for the group (NodeChip has both): the road owns selection for every card on the board, so a second source of truth would fight it. Recorded in the prop doc rather than silently. Checked component by component against the DS contracts, NodeChip and TreeRow are already at DS parity — the DS leaves the self-explanatory props bare too — so VersionedGroup was the whole gap. TOKENS STILL NOT RE-HASHED; the 2026-08-16 check remains the last one.",
  "verifiedAtPrevious": "2026-08-16: all seven token files AND kn-theme.css re-fetched from the DS and hashed AT THE SOURCE — every one byte-identical to the fingerprint recorded on 2026-08-12, so nothing has moved upstream. spacing.css differs from our copy only by the recorded --road-head-h hold. This is the check the fingerprintProvenance note below demands (hash the source, never the copy) and it came back clean; the 2026-08-16 movement was entirely in components.",
  "tokens": {
    "vendoredInto": "src/tokens/",
    "verbatim": false,
    "note": "Token files are mostly verbatim copies but one has diverged. Hashes below are the last-verified DS fingerprints; re-read the DS via /design-sync to detect further DS changes. 2026-08-05 re-sync: colors.css adopted contrast pass; 2026-08-08: typography.css adopted the 2026-08-05 DS addition (--ls-caps: 0.12em added, --ls-eyebrow aliased to it — every all-caps string in the system carries --ls-caps, a system-level rule not a per-component one). 2026-08-09 re-sync: all 7 files re-hashed and the recipe pinned down (see hashRecipe) — base/colors/elevation/fonts/motion reproduce their recorded fingerprints exactly, so they are unchanged and verbatim; typography.css is byte-identical to the DS and its NEEDS_RECOMPUTE marker is now a real hash. spacing.css keeps a LOCAL deviation, but the reason has INVERTED: the DS removed the dead --road-question-h / --road-tab-h (we converge) and corrected --road-head-h 28px -> 44px explicitly to adopt our old local value, except that value is now one revision stale — #70/#86 (2d59ac3) added the description row and grew the title, so AuthorRoad.tsx:84 headH() = HEAD_TITLE(28) + HEAD_DESC(20) + HEAD_COMBO(24) = 72px. We hold 72px; the road owns its own constants. Do NOT adopt 44 back. Re-reported on drift-log #74. 2026-08-11: --road-head-h is now DEAD in the app either way — the open card's head wraps, so AuthorRoad computes its height per card via headRows() and no source reads the token. The 44-vs-72 divergence is moot until something consumes it again. 2026-08-11 colors.css re-vendored: --border-hair 0.13 -> 0.17 and --border-rule 0.20 -> 0.25, restoring the file to byte-verbatim (every other line already matched). This was SILENT drift of the worst kind: the recorded fingerprint (89d70a85...) was the hash of our own stale copy, not of the DS file, so the mechanical check that exists precisely to catch this was vouching for the divergence. The DS flagged --border-hair; --border-rule was a second drift nobody had noticed. New fingerprint eec676c8... The hashRecipe was re-verified against base.css first, which still reproduces its recorded hash exactly. 2026-08-12 (#95 + #94), all seven re-fetched from the DS and hashed at the source — see fingerprintProvenance. Recipe proven first: 6 of the 7 local files reproduced their recorded hashes, so the tr/sha256 pipeline is sound and every mismatch below is real drift, not a machine artefact. THREE files had moved. (1) base.css was a whole revision behind: the DS replaced the single soft 8px bar with the THREE-STATE scrollbar (rest hidden / on 4px / near 7px with a wash and end arrows), driven by an assets/scrollbars.js that we did not have — a stylesheet whose states nothing could ever select. Adopted with its driver, since either half alone is inert; the diff was confined entirely to the scrollbar block, every line above it byte-identical. Its four SVG data-URI arrowheads were decoded and parsed rather than eyeballed (four 12px chevrons, stroke #c8bfaf at 1.6px). (2) elevation.css: --ring-linked 2px -> 1.5px, the softest ring thinned so correspondence reads quieter than commitment. A real visual change on every cross-pane hover. (3) fonts.css: comment-only, but it documents a LOAD-BEARING limit — JetBrains Mono ships at 400 and 500 ONLY, so a mono numeral asked for --fw-semibold or --fw-bold silently renders at 500. Weight steps on counts must be regular -> medium. Nothing in this app was relying on the heavier weights, but the constraint is now written down where a future mono numeral will meet it. 2026-08-19 (OB-012 + OB-014, run against DS state at 4bc13ea): all seven re-hashed, and SIX of the seven were re-fetched from the DS this run and diffed line by line rather than asserted. fonts.css, motion.css and typography.css were already byte-identical. colors.css and elevation.css had each drifted by a COMMENT ONLY and were re-vendored to byte-identical: colors.css gained the never-mix-raw-with-naturalised rule in its header (the H9/#69 finding, stated where a reader of the token file will meet it) and the --surface-sunken-2 explanation; elevation.css gained the nested-well alternation note. No VALUE moved in either. spacing.css was re-vendored under OB-014 with --road-pad and --rail-w REMOVED (PAD and RAIL_W are gone from src/, so both tokens named constants that no longer exist) and the DS's narrowed header adopted: the --road-* block is a RECORD, not a coupling, since nothing in src/ reads any --road-* token. --road-head-h stays 72px here against the DS's 44px, which the DS explicitly agrees we keep (#70/#86). base.css was NOT re-fetched this run: it was fetched from the DS and hash-verified one commit earlier at 4875ea8, with the pre-state check that proves the recipe, and nothing upstream reports it moved since. That is a one-day-old real fetch, not an assertion carried forward, but it is not this run's check either — recorded so the distinction survives. ONE INCONSISTENCY FOUND AND FIXED IN THIS TABLE: before this run, six entries were the hash of OUR LOCAL file and spacing.css's (c58475d8…) was the hash of the DS's file, which our local copy could never reproduce because it deviates on purpose. One row of the table meant something different from the other six and nothing said so — the same shape as the 2026-08-11 failure. Every entry is now OUR local file's fingerprint, uniformly; where a file deviates from the DS (spacing.css alone) the deviation is described above and in the file's own header.",
    "fingerprintProvenance": "2026-08-12 (#95): every hash below is now the sha256 of the DS FILE'S OWN BYTES, re-fetched via /design-sync and hashed one at a time. Before today most of them were not. They had been computed from OUR vendored copy, which makes the drift check circular: it can detect a local edit and can NEVER detect upstream movement, and it will report `verified` in exactly the case it exists to catch. That is how colors.css stayed behind for days while the check passed (see the 2026-08-11 note below), and re-running the audit properly found two more of the same: elevation.css and fonts.css had BOTH drifted while vouching for themselves. Two of the seven were already honest — spacing.css (c58475d8 reproduced from the DS bytes exactly) and, since yesterday's real comparison, colors.css. The rule from here: a fingerprint is only worth recording if it was taken from the source, never from the copy.",
    "hashAlgo": "sha256",
    "hashRecipe": "sha256 of the file's bytes with CRLF normalized to LF. This repo has core.autocrlf=true, so the working tree is CRLF while git blobs and the DS originals are LF — hashing the working-tree file raw yields a machine-local fingerprint that will never match the DS. Verify the recipe before trusting a mismatch: `tr -d '\\r' < src/tokens/base.css | sha256sum` must reproduce base.css below.",
    "files": {
      "base.css": "e6eba0a3732560e0997648be8578518c53759a648c7419761b78b68a365c31cd",
      "colors.css": "288c470239e089da9d9e62b94948d74108ece77951bbf42956fedd30e6114939",
      "elevation.css": "90db0f45e172fa9cf8944518058725ab8bd35bb9a078750d7318ca49aaac4d5f",
      "fonts.css": "79741c1697bb0a48464849018e98709638318be03442dfcc3c3254afe56e23fc",
      "motion.css": "a45296468f1f3649ecce810d01b3118b2cb3f4e5eb0eb6cb43bf329fe534c6eb",
      "spacing.css": "4093c06a50d415ab546c8956df9ed8bd0583460597528156269a99a4c55782f8",
      "typography.css": "b519debc149e58529528a055a48b2fcf8a234b6b1e8c59570e7fb9415eb3085e"
    }
  },
  "tailwindTheme": {
    "vendoredInto": "src/tokens/kn-theme.css",
    "from": "tailwind/kn-theme.css",
    "syncedAt": "2026-08-19",
    "verbatim": true,
    "hash": "f2c77ee72d74dea6dd0ef2113ec8719f08aef9325f11d1b74d1f7fb511906969",
    "hashRecipe": "same as tokens.hashRecipe — sha256 of the bytes with CRLF normalized to LF. Taken from the DS file, not from our copy",
    "note": "The DS's Tailwind v4 utility surface (#93), kept OUT of tokens.files on purpose: those seven are the token definitions the app consumes as var(--…), this is a generated @theme block that turns the same values into utility classes. Imported from src/index.css after the five definition files and before base.css. INSTALLED AS ADDITIVE: the standing warning was that it overrides Tailwind's --radius-* namespace and takes rounded-lg from 8px to 20px app-wide, but that change had ALREADY happened and nobody had noticed — spacing.css declares --radius-* on :root after `@import \"tailwindcss\"`, so it has been overriding Tailwind's defaults since the closure landed in #64 slice 5. Measured in the browser before and after this import: rounded-sm 10px, rounded-md 14px, rounded-lg 20px, unchanged either way. So kn-theme.css adds utilities (bg-canopy, text-ink-2, shadow-lift-1, text-body, font-display, border-hair, ease-soft, the drill-* animations) and moves no pixels. Nothing in the app consumes them yet; they are there so new work stops reaching for arbitrary values. #96's Studio shell is deliberately NOT adopted alongside it — the app frame and left rail stay slate until that is decided on its own. 2026-08-19 (OB-014): three COMMENT-ONLY drifts ported, no value moved and the declaration count is unchanged at 142. (1) w-[var(--rail-w)] is no longer offered as a layout-constant example, and the trailing block now says the --road-* values are a RECORD of AuthorRoad.tsx's const-s rather than \"LIVE layout constants — change one here, change it there\"; that instruction was wrong, because nothing in src/ reads a --road-* token. (2) the raw-hex block carries the never-mix-in-one-VIEW rule. (3) --color-sunken-2 carries the wells-alternate-by-depth note. Compared region by region against the DS source rather than byte-hashed at the source: this file is HAND-KEPT upstream (the DS's own github.md calls tokens/*.css → tailwind/kn-theme.css the only remaining hand-kept mirror, and says nothing checks it), so a matching hash would prove less here than it does for the seven.",
    "notVendored": [
      "tailwind/kn-theme.v3.js — the Tailwind v3 config form of the same theme; this app is on v4 (was export/tailwind/kn-theme.v3.js until the DS deleted export/ on 2026-08-18)",
      "GONE UPSTREAM: export/tokens/kn-base.css and export/tokens/kn-tokens.css, the single-file bundles of what src/tokens/*.css already carries file-by-file. They lived only inside export/ and were deleted with it on 2026-08-18; there is no replacement path and nothing here wanted one"
    ]
  },
  "assets": {
    "vendoredInto": "src/ds/assets/",
    "verbatim": true,
    "note": "DS material carried as-is rather than ported, so a re-sync is a copy and never a re-typing. scrollbars.js required allowJs in tsconfig.app.json for the same reason — porting it to .tsx would have made every future sync a manual merge.",
    "files": {
      "src/ds/assets/leaf-mask.png": {
        "from": "assets/leaf-mask.png",
        "syncedAt": "2026-08-11",
        "bytes": 31984,
        "note": "the leaf at half scale, alpha-only, authored by the DS expressly to fit get_file's 256 KiB cap. The full-size assets/leaf.png (1870x841) cannot cross the channel. Consumed by src/ds/chrome/LeafMark.tsx as a mask."
      },
      "src/ds/assets/scrollbars.js": {
        "from": "assets/scrollbars.js",
        "syncedAt": "2026-08-12",
        "deviations": [],
        "note": "the three-state scrollbar driver (#94). Byte-verbatim; it only sets data-sb, every pixel of the three states lives in tokens/base.css, so the two are one adoption and neither works alone. Imported for its side effect from src/main.tsx after index.css, matching the DS's own delivery note. CONSEQUENCE: it publishes window.PKT_SB = { FLASH, LEAVE, PAD }, which until now did not exist in this app — PaneHeader.tsx:37, NodeChip.tsx:50 and VersionedGroup.tsx:236/361 each read PKT_SB.LEAVE and fell back to a hard-coded 500. The DS ships LEAVE: 500, so no timing changes, but those four sites are now on the DS clock for real and the 'this app has no PKT_SB' deviation recorded against PaneHeader and VersionedGroup is retired."
      }
    }
  },
  "components": {
    "vendoredInto": "src/ds/",
    "verbatim": false,
    "kind": "port",
    "note": "Typed .tsx ports of the DS .jsx sources (NOT byte-identical — jsx→tsx adaptation). The prop contract each is ported against is the DS .d.ts. The DS `_adherence` config (raw hex / px / non-token font bans) is the REFERENCE SPEC only — it lives in the DS project, is not vendored here, and oxlint was declined (#65); #61 authored those bans as native ESLint `no-restricted-syntax` rules in eslint.config.js, SCOPED to token-clean files (src/App.tsx, src/studio/StudioView.tsx, src/studio/AppToolbar.tsx) with src/ds/** exempt, and grows per migrated file — it is NOT yet a repo-wide error. Drift-check a port by re-reading its `from` source and comparing behaviour. 2026-08-05 full re-diff of all 17 ports against the DS: 9 CLEAN (CountBadge, AppHeader, DomainDot, TrailChip, StepDot, WalkCard, BinMark, PillButton, Toolbar); 6 DRIFTED and adopted (TreeRow drawn caret + exports caretStyle; InstrumentRow drawn bullet + exports bulletStyle; EdgeLegend crispEdges EdgeDash; NodeChip lit shadow stacks ring+lift; SectionLabel count --fw-medium; PresetButton inactive border --border-rule; DocHeader title colour fallback); PaneHeader adopted the auto-hide ✕ drift. InstrumentGroup newly ported. Recurring DS direction: text-glyph marks → drawn geometry, and sub-pixel hardening. eslint.config.js relaxes react-refresh/only-export-components for src/ds/** because DS components ship colocated style helpers (caretStyle, bulletStyle, FamilyColumn).",
    "ported": {
      "src/ds/graph/NodeArrow.tsx": {
        "from": "components/graph/NodeArrow.jsx",
        "contract": "components/graph/NodeArrow.d.ts",
        "syncedAt": "2026-08-08",
        "deviations": []
      },
      "src/ds/graph/NodeChip.tsx": {
        "from": "components/graph/NodeChip.jsx",
        "contract": "components/graph/NodeChip.d.ts",
        "syncedAt": "2026-08-17",
        "deviations": [
          "2026-08-17: the DS corrected `onDelete`'s contract — it had said the ✕ “hovers to berry”, which would leave a port's delete marks neutral until hovered, backwards for a destructive control. THIS PORT WAS ALREADY RIGHT (--state-danger at rest, hovering within its own ramp — NodeChip.tsx:551) because it was ported from the .jsx under the DS's own rule that the .jsx is the truth and a disagreeing .d.ts is a bug to report. No change needed; recorded because it is the third contract-lag caught this way, after NodeChip.wrap and PillButton.onMouseDown.",
          "2026-08-16 LOCAL, ★ in the source: `wrap` WINS OVER A TOLD HEIGHT. Upstream is `alignItems: size && size.h ? 'center' : (wrap ? 'flex-start' : 'center')` — verbatim as of the 2026-08-16 .jsx, so this is a DS defect rather than a port slip. The component contradicts itself: when `wrap` is on it nudges every piece of furniture DOWN (the dot by 6, the index by 1, the ✕ by 1 AND with an `alignSelf: 'flex-start'` of its own), and those nudges are corrections for a TOP-aligned row. Told a height, the container switches to `center` while the ✕'s own alignSelf still tops out — alignSelf outranks alignItems — so one chip aligns two ways at once and the nudges push the rest further from where either rule wanted them. The road TELLS every leaf its height, so on the board that is every wrapped chip; it was invisible until #97 stopped the titles being clipped to one line. NEITHER BRANCH IS RIGHT, THOUGH, and that is the substance of the report: the two spans have different line boxes (--fs-micro 11 × --lh-snug = 14.85 against --fs-body 13 × --lh-snug = 17.55), and no top- or centre-alignment makes two unequal boxes share a baseline. MEASURED against the title's FIRST line, which is the line the number belongs on at any length: `flex-start` is a constant -3px on every chip; `center` is -2.14 at one line and drifts with the block, +6.63 at two and +15.41 at three; `baseline` is 0.00 at one, two and three lines, told and natural alike. So this port ships `alignItems: wrap ? 'baseline' : 'center'` — which is what CSS baseline alignment is for and what the contract already describes (`wrap`: \"its dot aligns to the first line, and the box grows down\"). Safe here because the ✕ carries its own alignSelf and mark=\"border\" has no dot; a dot-form chip would need its 6px marginTop revisited, which is the DS's call. The box does not change size — shot-foldab still predicts every chip exactly — and the driver now asserts the BASELINE rather than the CSS value, on both the told and the natural chip, so it reports the DS's own untold path as failing too. To report on #74: the DS has not fixed this in either branch.",
          "2026-08-16 LOCAL: PUBLISHED GEOMETRY — CHIP_METRICS / chipSize / ChipGeometry, the group's own pattern applied to this component because the DS ships none for it and keeps its measure()/linesOf() module-private (#97). A board that lays out arithmetically has to predict a chip's box before it renders, and AuthorRoad was guessing it: CHAR_W = 8 for a proportional face, chrome scored at 40 when it is really ~83, so every title the estimate scored as one line wrapped to two and was cropped by the shell's overflow:hidden against the height it had been TOLD. Two rules make the fix hold, both taken from the group's geometry: CSS OWNS WHAT CSS DECLARES (--stroke-rule, --lh-snug, --fs-body, --fs-micro and the weights are read from the stylesheet at measure time, never copied), and THE TABLE OWNS WHAT JS DECLARES — the RENDERER now reads CHIP_METRICS back for its gap, paddings, dot, index lift, title gutter and delete box, so a change lands in the drawing and in the prediction together or not at all. Text is canvas-measured at the chip's own font; fontOf/measure/linesOf are this file's own copies of the group's rather than a shared import, matching the DS's own call on useRecede/useSizeDrag. `maxHeight` is spent on LINES rather than cropped to, so the box returned always matches the lines actually drawn. PROVEN by tools/studio-spike/shot-foldab.mjs, which renders the chip both TOLD the predicted box (which must hold its text — zero overflow past the shell) and NATURAL (sizing itself inside the road's bound), over the titles that clipped on the board: told box exact, lines exact, title column within 1px and on the safe side — the canvas runs ~0.99px OVER the laid-out width of the mono step number, constant rather than per-character, so it under-claims the column and can only ever buy a line, never crop one. To report on #74: the DS should own this, as it already does for the group.",
          "2026-08-16: RE-PORTED, two DS revisions behind. Took `selectable` / `selected` / `defaultSelected` / `onSelectedChange` (2026-08-16 — a 2px --state-selected OUTLINE at 2px offset, OUTSIDE the domain border rather than replacing it, so a picked network node is still visibly a network node; it replaces the `lit` ring, one ring per meaning), controlled `width` / `height` / `onResize` (2026-08-11 — the drag runs on own state and reports on pointer-up, at rest the prop wins), and `mark: 'none'` (no disc and no hue, for a surface where domain is not the channel being read). The layout-correction effect now bails on a told size, which is what stops a controlled chip fighting its prop every frame. CONSUMED the same day (#97): the road's leaf stops ARE this component now — `mark=\"border\"` + `wrap`, told their box (width/height, resizable=false), taking their hue from --domain-* instead of the corpus's raw authored hex. The road keeps behaviour (position, relayout transition, drag, hover bus, the selection SET) and hands over the drawing; `selected` is passed WITHOUT `selectable`, which is the DS's own advice for a board — it paints the chip picked without handing it a click that would fight the marquee. The road's own delete ✕ (`browserBar`) and its `DOMAIN_COLOR` import are DELETED; `dim` is now the DS's off-the-resolved-path treatment (no lift, no fill) rather than a plain opacity-50",
          "2026-08-16 LOCAL: `optional` prop — dashes the chip's own border, hue kept, because \"dashed always means conditional\" is a system rule (readme, Borders and dashes) and an optional stop is the road's commonest conditional. The DS has no conditional form at all. A PROP rather than a stylesheet override from the host, and the reason is worth keeping: the border is written inline as a shorthand, so the only way in from outside is `border-style: dashed !important` on a descendant selector — which stops matching the day the component gains a wrapper element, SILENTLY, with every optional stop rendering solid and no test failing. Same class of trap as a fingerprint hashed from the copy. Same pattern as VersionedGroup's `optional`; drift-log #74, and the DS should own the form",
          "2026-08-05: lit boxShadow now stacks the ring under the lift (var(--ring-linked), var(--lift-1)) to match the DS — a lit chip keeps its elevation",
          "2026-08-08: added index (mono step number), mark ('dot'|'border'), onDelete (berry ✕ on hover), resizable+bounds — matching the 2026-08-07 DS update",
          "2026-08-11: restored `wrap` — this port had dropped it as undocumented; the DS corrected the .d.ts (its 2026-08-09 sync: a contract that lags its implementation silently loses behaviour), so the drop is no longer a deviation",
          "2026-08-19 OB-015: `resizable` now defaults to FALSE, matching the DS's 2026-08-18 change, and the DS's two-axes header note came with it — what a chip IS (`mark`, `lit`, `dim`, `wrap`) versus what can be DONE to it (`selectable`, `resizable`, `onDelete`, all opt-in, all working on every form). `selectable`'s doc already matched upstream. NOT a full re-port of the file: only the default and the two doc blocks moved, per the obligation's own scope (\"the prop's type and meaning are unchanged; only the default moved\"). NOTE, unported and out of scope: the DS's `mark` has gained a 'border-2' rank that this port does not carry."
        ]
      },
      "src/ds/graph/NodeChain.tsx": {
        "from": "components/graph/NodeChain.jsx",
        "contract": "components/graph/NodeChain.d.ts",
        "syncedAt": "2026-08-08",
        "deviations": []
      },
      "src/ds/graph/DomainDot.tsx": {
        "from": "components/graph/DomainDot.jsx",
        "contract": "components/graph/DomainDot.d.ts",
        "syncedAt": "2026-07-30",
        "deviations": []
      },
      "src/ds/graph/EdgeLegend.tsx": {
        "from": "components/graph/EdgeLegend.jsx",
        "contract": "components/graph/EdgeLegend.d.ts",
        "syncedAt": "2026-08-05",
        "deviations": [
          "2026-08-05: adopted the DS EdgeDash (an SVG rect with shapeRendering=crispEdges) in place of the CSS bar, and now exports it — fixes dashes antialiasing to different thicknesses per row"
        ]
      },
      "src/ds/graph/EdgeEntry.tsx": {
        "from": "components/graph/EdgeEntry.jsx",
        "contract": "components/graph/EdgeEntry.d.ts",
        "syncedAt": "2026-08-11",
        "deviations": [
          "reuses DOMAIN_TOKEN from ./vocab instead of an inline DOMAIN map (single source for the domain→token map). The EDGE map stays local, as it is in the DS module and in EdgeLegend"
        ]
      },
      "src/ds/nav/TreeRow.tsx": {
        "from": "components/nav/TreeRow.jsx",
        "contract": "components/nav/TreeRow.d.ts",
        "syncedAt": "2026-08-17",
        "deviations": [
          "2026-08-17: the DS contracted `caretStyle` at last — exported by its .jsx, imported by two DS components, re-exported by this port and imported by AuthorRoad, and absent from every .d.ts the whole time — and documented `depth` as the SYSTEM's nesting reference (16px caret slot + 16px per level, matched by anything else that nests). Both ported as prose. The longhands rule that travels with caretStyle (set `borderRight`/`borderBottom`, never `border` + `borderTop:'none'`: React diffs style key by key, so a re-render that changes only the shorthand's colour re-sets `border` and never re-sets the two switched-off sides, and the caret fills in to a diamond the first time it is hovered) was ALREADY satisfied here — grep finds no repeat of the recipe in src/. Also dropped the stale ▾/▸ from the `container` prose: the drawn caret is the system's only disclosure.",
          "reuses DomainCode/DOMAIN_TOKEN from ../graph/vocab instead of a local DOMAIN map (single source for the domain→token map)",
          "2026-08-05: adopted the DS drawn disclosure caret and now exports caretStyle (was a text ▾/▸ glyph) — InstrumentGroup imports it"
        ]
      },
      "src/ds/nav/TrailChip.tsx": {
        "from": "components/nav/TrailChip.jsx",
        "contract": "components/nav/TrailChip.d.ts",
        "syncedAt": "2026-07-31",
        "deviations": [
          "reuses DomainCode/DOMAIN_TOKEN from ../graph/vocab instead of a local DOMAIN map"
        ]
      },
      "src/ds/nav/StepDot.tsx": {
        "from": "components/nav/StepDot.jsx",
        "contract": "components/nav/StepDot.d.ts",
        "syncedAt": "2026-07-31",
        "deviations": []
      },
      "src/ds/nav/WalkCard.tsx": {
        "from": "components/nav/WalkCard.jsx",
        "contract": "components/nav/WalkCard.d.ts",
        "syncedAt": "2026-07-31",
        "deviations": []
      },
      "src/ds/doc/DocHeader.tsx": {
        "from": "components/doc/DocHeader.jsx",
        "contract": "components/doc/DocHeader.d.ts",
        "syncedAt": "2026-08-05",
        "deviations": [
          "reuses DomainCode/DOMAIN_TOKEN from ../graph/vocab instead of a local DOMAIN map",
          "2026-08-05: restored the DS title-colour fallback (|| var(--text-1))"
        ]
      },
      "src/ds/doc/SectionLabel.tsx": {
        "from": "components/doc/SectionLabel.jsx",
        "contract": "components/doc/SectionLabel.d.ts",
        "syncedAt": "2026-08-11",
        "deviations": [
          "2026-08-05: count weight --fw-regular → --fw-medium to match the DS (its comment mandates one step, not three)",
          "2026-08-11: the `action` slot is NO LONGER a deviation — this port took it from the jsx while the .d.ts was silent, and the DS has since documented it in SectionLabel.d.ts ('a control pushed to the trailing edge — use this instead of hand-rolling a head row'). Same correction class as NodeChip.wrap and PillButton.onMouseDown"
        ]
      },
      "src/ds/sidebar/PresetButton.tsx": {
        "from": "components/sidebar/PresetButton.jsx",
        "contract": "components/sidebar/PresetButton.d.ts",
        "syncedAt": "2026-08-05",
        "deviations": [
          "2026-08-05: inactive border is always --border-rule (was hair→rule on hover) to match the DS — hover changes only the background"
        ]
      },
      "src/ds/sidebar/InstrumentRow.tsx": {
        "from": "components/sidebar/InstrumentRow.jsx",
        "contract": "components/sidebar/InstrumentRow.d.ts",
        "syncedAt": "2026-08-05",
        "deviations": [
          "consumer drops the old flat-list composition-order index (the DS row has no such slot)",
          "2026-08-05: adopted the DS drawn state bullet and now exports bulletStyle (was a text ●/○ glyph)"
        ]
      },
      "src/ds/sidebar/InstrumentGroup.tsx": {
        "from": "components/sidebar/InstrumentGroup.jsx",
        "contract": "components/sidebar/InstrumentGroup.d.ts",
        "syncedAt": "2026-08-05",
        "deviations": [
          "also exports FamilyColumn(labels), matching the DS module; imports caretStyle from ../nav/TreeRow so the palette and the tree draw nesting the same way"
        ]
      },
      "src/ds/sidebar/BinMark.tsx": {
        "from": "components/sidebar/BinMark.jsx",
        "contract": "components/sidebar/BinMark.d.ts",
        "syncedAt": "2026-07-31",
        "deviations": []
      },
      "src/ds/chrome/PillButton.tsx": {
        "from": "components/chrome/PillButton.jsx",
        "contract": "components/chrome/PillButton.d.ts",
        "syncedAt": "2026-08-11",
        "deviations": [
          "2026-08-11: restored `onMouseDown` — dropped earlier as undocumented; the DS corrected the .d.ts (2026-08-09 sync), so the drop is no longer a deviation"
        ]
      },
      "src/ds/chrome/LeafMark.tsx": {
        "from": "components/chrome/LeafMark.jsx",
        "contract": "components/chrome/LeafMark.d.ts",
        "syncedAt": "2026-08-11",
        "deviations": [
          "drops the DS `base` prop (a path to the project root, which the DS needs because its pages have no build step) in favour of `import leafUrl from '../assets/leaf-mask.png'`, so Vite resolves and fingerprints the asset like every other one in this app",
          "masks with assets/leaf-mask.png rather than assets/leaf.png — the same drawing at half scale with the colour channels flattened. The component only ever reads the alpha channel, so nothing is lost, and the full-size PNG (1870×841) cannot cross the design-sync channel's 256 KiB get_file cap. RATIO and TIP_Y were measured on the full-size artwork and still hold at half scale"
        ]
      },
      "src/ds/chrome/CountBadge.tsx": {
        "from": "components/chrome/CountBadge.jsx",
        "contract": "components/chrome/CountBadge.d.ts",
        "syncedAt": "2026-07-31",
        "deviations": []
      },
      "src/ds/chrome/PaneHeader.tsx": {
        "from": "components/chrome/PaneHeader.jsx",
        "contract": "components/chrome/PaneHeader.d.ts",
        "syncedAt": "2026-08-19",
        "deviations": [
          "2026-08-17 GUARDED: the three pane rules now have a machine half — src/ds/adherence.test.ts. Rule 2 was the reason: it had been written down, read, and was STILL broken in five more pane bodies than the one the DS named, so a rule that lives only in prose was measured at roughly one-in-six compliance here. The sweep found 16 full-height elements painting a background across the instruments; 5 were `bg-white` (#ffffff) sitting inside a #fdfcfa pane and are FIXED here (KnowledgePanel, TreePanel, LensPane and both inline wrappers in studio/instruments.tsx); 10 are `bg-slate-50` in the un-migrated alt-visualizations, held as recorded debt because those instruments float white overlay chips on a slate canvas and stripping it would leave them near-white on near-white — that is the #69 migration, not this rule. The 16th, NestedAtlasView.tsx:528, paints #eef4f8 as the atlas's WATER and is listed for the same reason a body that means to paint should have to say so. Two things worth keeping from building it: (a) the guard was written against classNames and called the atlas clean, because that background is an INLINE STYLE — only a computed-style read off the running page caught it, so the check now reads both; (b) every rule here was mutation-tested, and three of the four guards did not fire on their first version (a `\\\\s*` before a negative lookahead backtracks to zero, so `background: 'none'` read as a violation, and `\\\\brounded\\\\b` matches inside `rounded-b-lg`, which let every bottom-only-radius violation clear itself). A guard that has never been made to fail is not evidence.",
          "2026-08-17: ported the three “what the pane around it must do” rules the DS moved out of its readme into PaneHeader.d.ts — the pane is position:relative with the --border-frame hairline and --radius-lg corners; THE BODY TAKES NO BACKGROUND OF ITS OWN and, if it clips, clips all four corners; a scrolling body insets 12px from the bottom. All three were broken in this app and are FIXED in the same branch (the pane body no longer paints --surface-canopy in walkdesk/RailroadView.tsx, StudioView's content div clips all four corners, and the road's scroller carries marginBottom var(--space-3)) — that is an adoption rather than a port, so it rides in its own commit behind its own issue. Also ported the icon-button recipe onto `actions`. RULED 2026-08-17: the legend title's padding moves to the DS's `0 5px`. This port had `0 8px` from #64 slice 3 (d4bc1b8), never recorded as deliberate — so it was silent drift, not a decision, and nothing here argued for the wider mask.",
          "2026-08-05: adopted the DS auto-hide ✕ — hidden at rest, revealed on pointer/focus inside the pane (binds to the parent pane element), ✕ box 20→18, title 'close'. The DS reads window.PKT_SB.LEAVE for the grace period; RETIRED 2026-08-12 — #94 vendored assets/scrollbars.js, which publishes PKT_SB, so this reads the DS clock rather than the 500ms fallback (same value)",
          "2026-08-19 RE-PORTED with OB-011, three changes. (1) PaneFrameContext is declared here and consumed here: the header now takes the frame Pane hands down, and falls back to the parentElement walk for a pane composed by hand. That walk made \"PaneHeader must be a direct child of the frame\" a rule a wrapper div could break in SILENCE — the reveal scoped to the header strip, so the close control simply never appeared and nothing said why. Inside a Pane there is no such rule left. (2) `grabbable` / `onGrabStart`: the title becomes a drag handle, the header owning the affordance (grab cursor, touch-action, user-select, the grabbing state cleared on the WINDOW because the pointer is elsewhere when a drag ends) and the host owning the position. It fires and stops — no pointer capture, no move/up listeners — so an existing gesture loop is untouched. (3) BOTH hand-written close buttons are gone; the legend and bar variants now render IconButton. That local iconStyle/hoverOn/hoverOff trio was the fourth of the five copies the DS standardised, and it lacked the reserved-border-at-rest guarantee's sibling behaviours — focus did not count as hover, so the control was reachable by pointer only.\n\nTHIS MEANS OB-013 IS ALREADY BUILT UPSTREAM, which the DS's own ledger does not say. design-sync.md still lists OB-013 as `needs: nothing yet — waiting on THIS side` and predicts the shape will be \"a single titleProps spread, not a named onTitlePointerDown\". The built shape is neither: it is a `grabbable` boolean plus `onGrabStart`. Ported as built and reported.\n\nsrc/ui/FloatingPanel.tsx STILL CARRIES ITS FORK. The prop it was waiting for now exists here, so the fork can end — but its hat is title-only (no close control, no actions) and its cursor is `move` where PaneHeader's is `grab`/`grabbing`, so adopting it changes what the user sees and belongs in its own change with its own look. Recorded rather than done.",
          "2026-08-19 ADOPTED by src/ui/FloatingPanel.tsx — OB-013, and the first adoption rather than port in this sequence. The panel had hand-written the whole legend hat, forked ONLY because the header could not make its title grabbable; that prop landed upstream on 2026-08-18, so the copy (its `cut` mask, its `over` layer, its title span and the `0 5px` padding that had once drifted to `0 8px`) is deleted and the component owns the notch, the mask, the type and the cursor. The host keeps the position, which is the division of labour the prop exists to draw.\n\nTHREE THINGS THAT MADE THE ADOPTION SAFE, each of which would have been a silent defect:\n(1) `legendBg` is REQUIRED here and is not the default. PaneHeader masks the border it interrupts with the colour BEHIND the pane, which defaults to --surface-canopy for a docked pane. This panel floats on --surface-raised, so the default would paint a canopy-coloured notch across its own top border. Passed explicitly.\n(2) The z-index survives. The fork set `zIndex: 2` on its handle so the title wins over the 4px 'n' resize strip beneath it — two affordances on one edge. PaneHeader's inner row carries `zIndex: 2` for its own reasons, and its root is `position: relative` with z-index auto, so it creates no stacking context and the 2 still competes in the panel's context. Behaviour preserved by luck of a matching number, so it is written down here rather than left to be rediscovered.\n(3) `PaneFrameContext` has no provider here, so PaneHeader falls back to the parentElement walk — which is correct: it is a direct child of the panel frame, and the walk resolves to exactly the element whose presence should reveal a close control. The panel passes no `onClose`, so nothing depends on it today.\n\nONE DELIBERATE BEHAVIOUR CHANGE: the title's cursor was `move` and is now `grab`/`grabbing`, the component's. Taken on purpose — the whole point of the item is that the header owns the affordance — and flagged to the DS in case `move` was a considered choice for a panel rather than a pane. The bottom move-grip keeps `cursor: move`: it is a grip, not a title.\n\nONE COST, REPORTED: the fork carried `aria-label=\"floating-panel-handle\"`, which tools/floating-panel-spike/drive.mjs used to locate the drag handle. PaneHeader gives a host no stable hook of its own, so that driver now locates the title by text. Verified rather than assumed: drive.mjs passes all five of its assertions after the swap — drag, resize, clamp, persist and auto-hide — so real pointer input still flows through onGrabStart into the panel's own gesture loop."
        ]
      },
      "src/ds/chrome/Toolbar.tsx": {
        "from": "components/chrome/Toolbar.jsx",
        "contract": "components/chrome/Toolbar.d.ts",
        "syncedAt": "2026-07-31",
        "deviations": []
      },
      "src/ds/chrome/AppHeader.tsx": {
        "from": "components/chrome/AppHeader.jsx",
        "contract": "components/chrome/AppHeader.d.ts",
        "syncedAt": "2026-07-31",
        "deviations": [
          "reuses DomainCode/DOMAIN_TOKEN from ../graph/vocab instead of a local DOMAIN map (single source for the domain→token map)"
        ]
      },
      "src/ds/group/VersionedGroup.tsx": {
        "from": "components/group/VersionedGroup.jsx",
        "contract": "components/group/VersionedGroup.d.ts",
        "syncedAt": "2026-08-17",
        "deviations": [
          "2026-08-17 DOC PORT: the 30 props that carried no doc comment took the DS .d.ts prose verbatim (12/50 documented -> 42/50). This port was a WORSE MANUAL than the source it was copied from, in the one file anyone here opens instead of the DS. Guarded now by src/ds/adherence.test.ts, which counts undocumented props across src/ds and pins the number, so porting more prose has to lower it deliberately. The `selected` doc was found stale in the same pass — it described the head-only 1px ring the DS replaced on 2026-08-16, while the code beside it had already been re-ported to the whole-card 2px outline. `defaultSelected` / `onSelectedChange` remain unported ON PURPOSE (the road owns selection board-wide); that is now stated in the prop doc rather than left to be rediscovered.",
          "2026-08-17: RE-SYNCED to the DS revision that renamed `ungroupBlockedLabel` -> `ungroupConfirmLabel` (old name still honoured, marked @deprecated) and corrected `onClose`, which had gone on documenting the WITHDRAWN refusal — the F1 self-contradiction this port reported on #74, now fixed upstream. BOTH CALL SITES in AuthorRoad moved and were RE-WORDED: they passed text written for the refusal (“cannot ungroup — N versions live here; delete all but one first”) into a panel that now ASKS, so the shipped road printed a sentence saying the act was impossible directly above a button that performs it. Caught by the DS looking at our screen; no guard here would have caught it, since the string is a prop value.",
          "2026-08-16 LOCAL: `depth` PROP, falling back to the DS's context. The DS steps the well's tint by nesting depth (--surface-sunken even, --surface-sunken-2 odd) and counts it through a React context, deliberately offering no prop — sync-log.md, then named export/SYNC.md: \"a group cannot be told its depth by a caller that does not know how it is being composed\". That is right, and it INVERTS in `bodySlot` mode, which the DS built for this road and names as such: the board floats its cards as board-level siblings (so a click inside a node never bubbles into a parent group), which means it is not their React ancestor, the context never arrives, every card reads depth 0, and every well paints the same tint — the exact \"a well inside a well of one tint read as one well with loose furniture in it\" the alternation exists to prevent. Such a board is also the ONLY thing that knows the depth: AuthorRoad has carried `Placed.depth` all along. Implemented as `depthProp === undefined ? useContext(Depth) : depthProp`, so ordinary nesting is untouched and only a caller that broke the tree on purpose supplies what the tree cannot. PROVEN by shot-foldab's new `nested` case (depth 1) and a tint assertion that resolves both tokens and compares: severing the prop turns it red (painted rgb(234,228,217), wanted rgb(226,219,205)). To report on #74 — this is the THIRD thing bodySlot hosting cannot receive, after the chip's size and the body cap, and they are one report: the DS assumes you render its children inside it.",
          "2026-08-16 HOST RULE (#97): a `bodySlot` host must pass bodyMaxHeight=\"none\". The prop defaults to 260 and caps the body with overflow-y:auto, but openHeight() does NOT model the cap — it returns the height the slot ASKED for. So the road reserved 464 for a card that drew 365, and because bodySlot steps are board-level siblings they do not scroll with the body: they hung out of the bottom of the card while the reserved height left dead space above the arrow out of it. Passing the slot height instead is also wrong — the cap applies to the body BOX, padding included, so a tight number crushes it by that padding (-6 on every open case in shot-foldab, and -30 on the empty version, whose zone the component draws at GROUP_METRICS.emptyZone whatever was asked for). The foldab page now carries a `tall` case (slot 355) so this cannot go unnoticed again: every earlier open case asked for 94 or 34, both under the default, which is why that driver reported +0 while the road was shipping the bug. To report on #74: openHeight should model bodyMaxHeight, or the contract should say a bodySlot host has to clear it. Note also that the resize-drag seed casts the prop to number (VersionedGroup.tsx:841), so \"none\" would be NaN there — harmless only because a bodySlot host passes resizable={false}.",
          "2026-08-16 SECOND SYNC, same day: the DS revised the component again AFTER the morning's import, and the re-read found five more changes plus two moved metrics. (1) GROUP_METRICS foldPadX 9 -> 8 and bodyPadRight 4 -> 8 — the second moves the road's own SLOT_RIGHT from 13 to 17, since it is derived. (2) The fold's horizontal padding is --space-2 in BOTH states and the peek's 6px strip is reserved OPEN as well, so the title's column no longer changes width when the card folds. (3) The well tint alternates by depth through a `Depth` context (--surface-sunken even, --surface-sunken-2 odd) — a well inside a well of one tint read as one well with loose furniture in it. (4) `selected` moved from the HEAD ONLY to the whole card, open or folded, at 2px offset instead of 1: the head-only ring 'reads as a stray box inside a card rather than as a card that is picked'. Our port had the head-only reading and the extra wrapper div it needed; both are gone. (5) Ungrouping a multi-version group is CONFIRMED, not refused — a portaled panel naming the count with keep/ungroup answers, on the same portalTarget as the menu, because refusing 'does not save the work, it only makes the user delete the versions one at a time first'. (6) `numberScope` + `localIndex`: a group shows its own ordinal and numbers its children from it at any depth; the chain gets no prefix unless numberScope='path'. PROVEN: shot-foldab still +0 (max 0.04 open, 0.01 folded) after the metric moves, and shot-cardhead's rewritten axis guard passes",
          "2026-08-16: the .d.ts CONTRADICTS ITSELF on ungrouping — `ungroupBlockedLabel` documents a confirmation while `onClose` still says 'Refused ... the component says so and does not call this' — and it omits `selected`, `onSelectedChange` and `numberScope` entirely, all of which the .jsx has. Ported from the .jsx per the DS's own rule that the .jsx is the truth. To report: same contract-lag class as NodeChip.wrap and PillButton.onMouseDown",
          "2026-08-16: RE-SYNCED to the DS revision that grew `bodySlot` / `slotHeight` / `onBodySlot` (chrome-only hosting for a board that floats its own nodes — the road's exact case, named as such in the DS source), the PUBLISHED GEOMETRY (GROUP_METRICS, headHeight, openHeight, foldedSize, GroupGeometry — canvas-measured text) and a `selected` prop of its own (folded: outline on the face; open: outline round the HEAD only, never the body). Applied as the DS's seven hunks over the port; every earlier port idiom kept",
          "2026-08-16: FULLY CONSUMED — the road's OPEN card is now the component in bodySlot mode (folded={false}, width, narrow, slotHeight, count all TOLD from the layout's spec), its steps floated into the reported slot; the road's own GroupHead.tsx (a transcription of the head rows), its board-level VersionMenu, delete-confirm popover and ungroup-refusal note are DELETED. So the checkStyle / VersionRow / AddRow / ConfirmButton / IconButton exports of 08-10..16 are RETIRED — all sub-components are private again, matching the DS layout",
          "2026-08-16 LOCAL: `optional` prop — the host's dashed cue, an outline hugging the face in both states (a conditional container is conditional as a whole), yielding to `selected` when folded. Not in the DS; drift-log #74",
          "2026-08-16 LOCAL, marked ★ in the geometry: the DS's functions as shipped predict short of what the DS component itself renders — the face's two 1px borders uncounted; the DescLine block is a 13px strut's line box (19.55 at one line, not 2 + 16.2; wrapped, 2.36 + n×16.19); the picker floors at 30 (padding + borders + its 18px cells), not 28; the narrow tally row is 18.84, not 14.85; the face's --space-1 gap before the body is missing from openHeight; and an empty version's zone is 58 tall under content-box, not the 34 asked for. Corrected via extra GROUP_METRICS keys (faceBorder, descStrut, descLastDescent, pickerCell, tallyRow, emptyZone, railStroke) and marked lines. PROVEN by tools/studio-spike/shot-foldab.mjs, which renders the port open (bodySlot) and folded at the road's widths and fails on any disagreement between GroupGeometry and the rendered card / the reported slot: +0 in every case. To report to the design agent as its own issue",
          "2026-08-16 HOST RULE: the DS renders content-box (its base.css has no universal box-sizing); the app is Tailwind border-box, and both the component (fold face 184 vs 190) and its geometry (foldedSize returns 196 = 190 + peek) assume the DS regime. Every hosted card sits under [data-ds-host] (src/index.css), which restores content-box on a subtree that is entirely DS ports. To report: a component whose geometry depends on the host's reset is the DS's own portability gap",
          "imports caretStyle from ../nav/TreeRow and bulletStyle from ../sidebar/InstrumentRow rather than redeclaring the drawn geometry, so the group, the tree and the palette draw disclosure the same way (the DS module declares its own copies)",
          "useRecede + useSizeDrag are separate copies from NodeChip's, not a shared hook — sharing would cross the group boundary. Kept parallel by design, as in the DS",
          "reads window.PKT_SB.LEAVE for the auto-hide grace period like the DS; RETIRED 2026-08-12 — #94 vendored assets/scrollbars.js, which publishes PKT_SB, so the fallback no longer fires (it was 500ms, the same value the DS publishes)",
          "all sub-components (NameField, IconButton, DescLine, RestoreMark, VersionRow, ConfirmButton, AddRow) are module-level and non-exported, matching the DS layout",
          "2026-08-10: prop contract re-verified 1:1 against the .d.ts — all 37 props present, same names and optionality",
          "2026-08-12: CONSUMED at last, on the road's CLOSED container cards (#91). A shut card hosts the whole component folded — the children-are-board-level-siblings wall only binds when there are visible children, and a fold has none. The road hands over the drawing and keeps the behaviour: the wrapper owns position, the relayout transition, drag, selection, hover-dim and double-click-to-open; the component owns the picture, the title, the tally, and the two controls (its RestoreMark maximises, its ✕ ungroups — askUngroup already implements the road's refuse-when-multiple-versions rule exactly). Hosted with narrow + width=NODEW + movable=false + resizable=false, so it is TOLD its box and never measures itself, which is what lets layoutRoad place it. The OPEN card still mirrors the head by hand, so this port is now half-consumed and checkStyle stays exported",
          "2026-08-12: the road GAVE UP ADR-0005 D2's two peek plates to take this. The DS stacks one well-tinted plate behind a fold per its own elevation.css rule; two pill silhouettes behind a rounded-lg card is nobody's drawing. The plural reading those plates carried now rides on the tally, in words. Measured cost: a one-line fold is 71px where the pill was 34. Shot side by side first (tools/studio-spike/foldab/), the evidence went to the design agent on #100, and the swap was landed anyway as a deliberate call — iterate from here rather than from a drawing nothing consumes",
          "2026-08-10: exports checkStyle — the railroad's mirrored head draws the same tick; the DS keeps it private. Reported on drift-log #74; retires if #91 lands a shared boundary",
          "2026-08-11: adopted the DS answer to #91 — controlled width/bodyHeight/offset/narrow (each read like `folded`; passing narrow uninstalls the ResizeObserver) and menuPortal (listbox rendered via createPortal, fixed-positioned from the picker's rect, flips up at the viewport edge)",
          "2026-08-11 (second read, same day): re-ported the menu after the DS revised it. menuPortal now DEFAULTS to document.body — an opt-in would have left every existing call site broken — with menuPortal={null} restoring the in-card listbox; the DS also corrected its own diagnosis (the trap is the group's own position:relative+z-index:1 shell, not the board, so two adjacent groups reproduce it with no canvas at all). Ported with it: the anchor re-measures on rAF after open and on a ResizeObserver over the picker (the card is still sizing in the commit that opens the menu), setAnchor bails on an unchanged rect, and the list is border-box so its padding comes out of the picker's width. menuPortal is now IN the .d.ts — the earlier contract-lag note is retired, and #91's needs:implementation is answered"
        ]
      },
      "src/ds/chrome/IconButton.tsx": {
        "from": "components/chrome/IconButton.jsx",
        "contract": "components/chrome/IconButton.d.ts",
        "syncedAt": "2026-08-19",
        "deviations": [
          "2026-08-19 NEW FILE, first half of OB-011 (#112). The DS asked for IconButton FIRST because it carries the recede clock and three components import from it. Ported whole: the component plus RECEDE_LEAVE_MS, recedeMs, subscribePresence, usePresence and useRecede. The .jsx and the .d.ts AGREE here — no contract gap to report, which is a first for this project.",
          "2026-08-19 REPLACES a private copy. `IconButton` was a local function inside VersionedGroup.tsx (the fifth of the five hand-written copies the DS standardised on 2026-08-17, and the one found only when the duplicate name broke their build). It is deleted; the two call sites now use the shared component.",
          "2026-08-19 THE CLOCK IS NOW IN ONE PLACE — this closes OB-008. Four files open-coded `?? 500`: PaneHeader (a hand-rolled presence effect on the pane), NodeChip (a private useRecede), and VersionedGroup twice (VersionRow's own copy, and the shell's presence effect). All four now import. PaneHeader takes usePresence(rootRef, { parent: true }) because the header is rendered BY the pane, so presence is the pane's; VersionedGroup's shell takes usePresence(shell) directly.",
          "2026-08-19 TWO CALL-SITE DECISIONS, both deliberate and neither a guess. (1) glyphSize is passed explicitly on the fold control: the shared component derives 10px at an 18px box, which is right for the ✕ but leaves – and the restore mark a step light, and the DS's own contract states the same pair (✕ at 10 where – and + sit at 12). Passing it keeps the rendered pixels identical to the private copy's 12. (2) tone stays 'chrome' on the ungroup ✕: ungrouping is a SPILL, not a delete — the nodes are spliced back onto the board, and count: 0 IS the delete case — so it is a neutral act and berry-at-rest would overstate it. Reasoned from design-sync.md's own 'Ungrouping is a spill' rule rather than read out of VersionedGroup.jsx, which was not fetched this run.",
          "2026-08-19 NO GEOMETRY MOVED. tools/studio-spike/shot-foldab.mjs was run after the swap and still agrees with the rendered card in every case (openHeight, bodyTop, foldedSize, chip sizes, well tint by depth). That is the expected result HERE: the geometry half of OB-011 — hairline() and GROUP_METRICS.pickerMarkLine — is NOT in this commit, and it is the part the DS says will and should turn that driver red. A green driver now means the extraction was clean; a green driver AFTER the geometry lands would mean that port did not take."
        ]
      },
      "src/ds/chrome/Pane.tsx": {
        "from": "components/chrome/Pane.jsx",
        "contract": "components/chrome/Pane.d.ts",
        "syncedAt": "2026-08-19",
        "deviations": [
          "2026-08-19 NEW FILE, second half of OB-011 (#112): Pane, PaneScroller and SCROLLER_INSET. PaneFrameContext is deliberately NOT here — it is declared in PaneHeader and imported by this file. Pane already imports PaneHeader, so the other direction is a cycle, and Pane.d.ts warns that a port siting the context here bundles cleanly and fails at RUNTIME with an undefined context (the close control never appears). Followed as written.",
          "2026-08-19 CONTRACT GAP, the fourth of its kind and the one this port would have lost. Pane.d.ts describes `grabbable` in PROSE (\"A FLOATING PANE is this component with grabbable\") but its PaneProps interface declares neither `grabbable` nor `onGrabStart`, while Pane.jsx forwards both to PaneHeader. Ported from the .jsx per the DS's own rule that the .jsx is the truth; reported in receipts. Same family as NodeChip.wrap and PillButton.onMouseDown, except that here the prose and the interface disagree inside ONE file.",
          "2026-08-19 NOT ADOPTED, and this is the whole point of the port/adopt split. Nothing in src/instruments/** or src/studio/** composes Pane yet; every pane on screen is still a hand-built frame around PaneHeader. This commit makes the adoption possible and does not perform it. OB-001 (H1) and OB-002 (H2) are already cleared by hand at their call sites, so adopting Pane is a make-it-unbreakable move rather than a fix — which is exactly why it should be its own reviewed change and not a rider on a port.",
          "2026-08-19 `as` is typed ElementType rather than the .d.ts's `string`. The .d.ts says `as?: string`, which is honest for a JS component but would reject `as={motion.div}` and gives up JSX checking on the frame element; ElementType is the TypeScript spelling of the same idea and accepts every value the .d.ts allows. A widening, not a divergence in behaviour.",
          "2026-08-19 `children` is documented on both interfaces where the DS leaves it bare, because src/ds/adherence.test.ts ratchets the undocumented-prop count and two bare `children` would have raised the budget from 56 to 58. Documenting was preferred to loosening the ratchet."
        ]
      }
    },
    "deferred": {
      "components/chrome/PaneDock": "Listed in issue #64's prose but has NO file in the DS project — nothing to port."
    }
  }
}
